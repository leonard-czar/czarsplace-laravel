# name: Czarsplace CI/CD

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   laravel-tests:
#     runs-on: ubuntu-latest
    
#     steps:
#     - uses: actions/checkout@v2
    
#     - name: Set up PHP
#       uses: shivammathur/setup-php@v2
#       with:
#         php-version: '8.2'

#     - name: Setup SSH
#       run: | 
#         mkdir -p ~/.ssh
#         echo "${{ secrets.SERVER_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/id_rsa
#         chmod 600 ~/.ssh/id_rsa
#         ssh-keyscan -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts
#         echo -e "Host *\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config



#     # - name: Test SSH Connection
#     #   run: ssh -i ~/.ssh/id_rsa ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_IP }} "echo SSH connection successful"

    

#     # - name: Install Dependencies
#     #   run: composer install --prefer-dist --no-progress --no-suggest

#     # - name: Run Tests
#     #   run: php artisan test

#   czarsplace-deploy:
#     needs: laravel-tests
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
#     steps:
#     - uses: actions/checkout@v2
    
#     - name: Deploy Laravel to Kali Linux server
#       uses: appleboy/ssh-action@master
#       with:
#         host: ${{ secrets.HOST }}
#         username: ${{ secrets.SERVER_USERNAME }}
#         key: ${{ secrets.SERVER_PRIVATE_KEY }}
#         script: |
#           cd  /home/lebechi/www/czarsplace.cybercraftsmen.tech/czarsplace-laravel
#           git pull origin main
#           composer install --no-interaction --prefer-dist --optimize-autoloader
#           php artisan migrate --force
#           php artisan config:cache
#           php artisan route:cache
#           # Add any additional deployment steps here





name: Czarsplace CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  laravel-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - name: Run tests
        run: |
          # Run your tests here

  czarsplace-deploy:
    needs: laravel-tests
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v2
      - name: Deploy Laravel to Kali Linux server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_PRIVATE_KEY }}
          script: |
            cd /home/lebechi/www/czarsplace.cybercraftsmen.tech/czarsplace-laravel
            composer install --no-interaction --prefer-dist --optimize-autoloader
            php artisan optimize:clear
            php artisan migrate --force
            php artisan config:cache
            php artisan route:cache
            # Add any additional deployment steps here

